diff --git a/system/zlib/Makefile b/system/zlib/Makefile
index b750f78..af235d7 100644
--- a/system/zlib/Makefile
+++ b/system/zlib/Makefile
@@ -29,24 +29,26 @@ CSRCS += zlib/contrib/minizip/zip.c
 
 CFLAGS += -Dunix -Wno-shadow -Wno-strict-prototypes -Wno-undef
 CFLAGS += ${INCDIR_PREFIX}zlib
+# gzlib/gzread use lseek/read prototypes guarded by Z_HAVE_UNISTD_H
+CFLAGS += -DZ_HAVE_UNISTD_H
 
 ZLIB_ZIP = zlib13.zip
 ZLIB_SRC = zlib
 
 ifeq ($(wildcard zlib/.git),)
 $(ZLIB_ZIP):
-	@echo "Downloading: $(ZLIB_ZIP)"
-	$(Q) curl -O -L https://github.com/madler/zlib/releases/download/v1.3/zlib13.zip
+        @echo "Downloading: $(ZLIB_ZIP)"
+        $(Q) curl -O -L https://github.com/madler/zlib/releases/download/v1.3/zlib13.zip
 
 $(ZLIB_SRC) : $(ZLIB_ZIP)
-	unzip -o $(ZLIB_ZIP)
-	mv zlib-1.3 $(ZLIB_SRC)
+        unzip -o $(ZLIB_ZIP)
+        mv zlib-1.3 $(ZLIB_SRC)
 
 context:: $(ZLIB_SRC)
 
 distclean::
-	$(call DELDIR, $(ZLIB_SRC))
-	$(call DELFILE, $(ZLIB_ZIP))
+        $(call DELDIR, $(ZLIB_SRC))
+        $(call DELFILE, $(ZLIB_ZIP))
 
 endif
 
